define('consts', [], function() {
    'use strict';

    const
        _DOC = window.document,
        _DOCBODY = _DOC.body,
        _WIN = window;

    return {
        DOC: _DOC,
        DOCBODY: _DOCBODY,
        WIN: _WIN
    }
});
